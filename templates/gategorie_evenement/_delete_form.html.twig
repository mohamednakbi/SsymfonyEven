<form method="post" action="{{ path('gategorie_evenement_delete', {'id': gategorie_evenement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gategorie_evenement.id) }}">
    <br>    
    <button class="btn btn-danger pull-right">Delete</button>
</form>
